// Recharts에 전달할 차트 색상 — globals.css chart 토큰 순서와 동일
export const CHART_COLORS = [
    'var(--chart-1)',
    'var(--chart-2)',
    'var(--chart-3)',
    'var(--chart-4)',
    'var(--chart-5)',
] as const;
